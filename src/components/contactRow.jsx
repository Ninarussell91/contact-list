export default function ContactRow((array.map contacts )) {
import React from 'react';

const ContactRow=({name,phone }) =>{


return (
<tr>
<td>{contact.name}</td>
<td>{contact.email}</td>
<td>{contact.phone}</td>

</tr>
);

}
